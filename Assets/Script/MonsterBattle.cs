using UnityEngine;

public class MonsterBattle : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private BaseMonster mon;
    private void Alive(){

    }
    private void Start()
    {
        
    }
    public void SetupBattle(bool isPlayerMonster){
        if(isPlayerMonster){
            
        }
    }
}
